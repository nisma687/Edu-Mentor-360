import {
    createBrowserRouter,
    
  } from "react-router-dom";
  const router = createBrowserRouter([
    {
      path: "/",
      element: <div>Hello EduMentor!</div>,
    },
  ]);

export default router;